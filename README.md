# .github
To Do List Planner holds the tasks you mean to finish on the PC you already operate, with a date written on each row. ToDoList keeps that list in one file, so the next morning starts from those same rows instead of a fresh pile of loose notes you already kept.
